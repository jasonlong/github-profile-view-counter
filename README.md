Forked from https://github.com/Gerhut/visitors.vercel.app

I've only customized the label ("views" instead of "visitors" since the same visitor can increment the counter) and the color (always blue from Primer).
